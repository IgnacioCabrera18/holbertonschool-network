#!/usr/bin/env bash
# Configures an Ubuntu server to change IP resolution for localhost and facebook.com

grep -v localhost /etc/hosts | grep -v facebook.com > /tmp/hosts_temp
echo "127.0.0.2 localhost" >> /tmp/hosts_temp
echo "8.8.8.8 facebook.com" >> /tmp/hosts_temp
cp /tmp/hosts_temp /etc/hosts